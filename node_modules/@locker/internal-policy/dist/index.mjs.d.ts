import { ScriptElement } from "@locker/shared-dom";
type Evaluator = (sourceText: string) => any;
// eslint-disable-next-line no-shadow
declare enum ContentType {
    HTML = 0,
    SVG = 1,
    XML = 2
}
declare const lwsInternalPolicy: {
    createHTML(dirty: string, lwsKey: string, contentType: ContentType): any;
    createScript(_dirty: string, _evaluator: Evaluator): string;
    createScriptURL(dirty: string, evaluator: Evaluator, targetElement: ScriptElement): string;
};
export { lwsInternalPolicy, Evaluator, ContentType };
//# sourceMappingURL=index.mjs.d.ts.map