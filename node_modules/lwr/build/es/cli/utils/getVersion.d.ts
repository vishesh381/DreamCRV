/**
 * This function needs to be in its own file so we can auto mock it properly.
 * (import.meta.url function doesn't play well with CJS/jest and causes test failures)
 * @returns package version
 */
export declare function getVersion(): string;
//# sourceMappingURL=getVersion.d.ts.map