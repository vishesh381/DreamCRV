import type { ViewProvider, ProviderContext, CompiledView, ViewIdentity, Watcher } from '@lwrjs/types';
import BaseViewProvider from '@lwrjs/base-view-provider';
export default class HtmlViewProvider extends BaseViewProvider implements ViewProvider {
    name: string;
    contentDir: string;
    layoutsDir: string;
    watcher?: Watcher;
    private emitter;
    private cachedViews;
    constructor(pluginConfig: unknown, providerConfig: ProviderContext);
    onModuleChange(fileChanged: string): Promise<void>;
    initialize(): Promise<void>;
    getView(viewId: ViewIdentity): Promise<CompiledView | undefined>;
}
//# sourceMappingURL=index.d.ts.map