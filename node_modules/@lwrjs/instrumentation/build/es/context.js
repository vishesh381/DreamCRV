import { createContextKey } from '@opentelemetry/api';
export const LWR_TRACE_CONTEXT = createContextKey('lwr');
/**
 * Get a new context key
 *
 * @remarks
 * Creates a singleton to generate unique context keys for nested contexts
 * https://opentelemetry.io/docs/instrumentation/js/context/#set-active-context
 */
export const getContextKey = (() => {
    let key = 0;
    return () => {
        return ++key;
    };
})();
//# sourceMappingURL=context.js.map