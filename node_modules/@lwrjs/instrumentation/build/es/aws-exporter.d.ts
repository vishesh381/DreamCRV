import type { SpanExporter, ReadableSpan } from '@opentelemetry/sdk-trace-base';
import { ExportResult } from '@opentelemetry/core';
type DoneCallback = (result: ExportResult) => void;
export declare function convertToReadableSpan(span: ReadableSpan): Record<string, any>;
export declare class AWSExporter implements SpanExporter {
    private metricsSender;
    private isShutdown;
    private isLambda;
    export(spans: ReadableSpan[], done: DoneCallback): void;
    log(spans: ReadableSpan[]): void;
    send(spans: ReadableSpan[], done: DoneCallback): void;
    shutdown(): Promise<void>;
}
export {};
//# sourceMappingURL=aws-exporter.d.ts.map