var __create = Object.create;
var __defProp = Object.defineProperty;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __markAsModule = (target) => __defProp(target, "__esModule", {value: true});
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, {get: all[name], enumerable: true});
};
var __exportStar = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, {get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable});
  }
  return target;
};
var __toModule = (module2) => {
  return __exportStar(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? {get: () => module2.default, enumerable: true} : {value: module2, enumerable: true})), module2);
};

// packages/@lwrjs/api/src/cli/exp-dev.ts
__markAsModule(exports);
__export(exports, {
  expDev: () => expDev
});
var import_chalk = __toModule(require("chalk"));
var import_diagnostics = __toModule(require("@lwrjs/diagnostics"));
var import_shared_utils = __toModule(require("@lwrjs/shared-utils"));
var import_setup_dev = __toModule(require("./setup-dev.cjs"));
async function expDev(options) {
  const {port, open, logLevel, mode, timeout, regenerateAllRoutes, writeSrcFilesToBundle} = options;
  process.env.LWR_LOG_LEVEL = logLevel;
  await (0, import_setup_dev.setupDev)(options);
  if (timeout) {
    process.env.SSR_TIMEOUT = `${timeout}`;
  }
  if (options.authToken) {
    process.env.AUTH_TOKEN = options.authToken;
    process.env.MRT_ALLOW_COOKIES = "true";
  }
  let serverUrl = `http://localhost:${port}`;
  if (options.basePath) {
    options.basePath = options.basePath.startsWith("/") ? options.basePath : `/${options.basePath}`;
    serverUrl += options.basePath;
  }
  serverUrl = serverUrl.endsWith("/") ? serverUrl : `${serverUrl}/`;
  process.env.MRT_HMR = "true";
  if (mode === "dev" && process.env.SSR_DEBUG === void 0)
    process.env.SSR_DEBUG = "true";
  if (mode === "dev" && process.env.SSR_WITH_CSR_FALLBACK === void 0)
    process.env.SSR_WITH_CSR_FALLBACK = "true";
  if (regenerateAllRoutes)
    process.env.WARMUP = "true";
  let runtimePort = port;
  let server;
  try {
    const {createDevServer} = await Promise.resolve().then(() => __toModule(require("../localdev/dev-server.cjs")));
    server = await createDevServer(options);
    await server.listen(async ({serverMode, port: appPort}) => {
      runtimePort = appPort;
    });
    console.log(import_chalk.default.green(`Site is available at: ${serverUrl}`));
    if (writeSrcFilesToBundle) {
      console.log(import_chalk.default.yellow(`WARNING: Any routes you navigate to in your browser will be compiled from local source and written directly into the MRT pre-built site. Use at your own risk!`));
    }
    if (open) {
      await (0, import_shared_utils.launch)(runtimePort, void 0, options.basePath);
    }
    return server;
  } catch (error) {
    import_diagnostics.logger.error(error);
    process.exit(1);
  }
}
