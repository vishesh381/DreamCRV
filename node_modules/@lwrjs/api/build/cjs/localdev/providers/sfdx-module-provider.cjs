var __create = Object.create;
var __defProp = Object.defineProperty;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __markAsModule = (target) => __defProp(target, "__esModule", {value: true});
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, {get: all[name], enumerable: true});
};
var __exportStar = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, {get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable});
  }
  return target;
};
var __toModule = (module2) => {
  return __exportStar(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? {get: () => module2.default, enumerable: true} : {value: module2, enumerable: true})), module2);
};

// packages/@lwrjs/api/src/localdev/providers/sfdx-module-provider.ts
__markAsModule(exports);
__export(exports, {
  default: () => sfdx_module_provider_default
});
var import_path = __toModule(require("path"));
var import_fs = __toModule(require("fs"));
var import_lwc_module_provider = __toModule(require("@lwrjs/lwc-module-provider"));
var import_shared_utils = __toModule(require("@lwrjs/shared-utils"));
var import_diagnostics = __toModule(require("@lwrjs/diagnostics"));
var SFDX_SCOPE = "c/";
var MODULE_DIR = import_path.default.join(process.cwd(), "force-app", "main", "default", "lwc");
var SFDXProvider = class extends import_lwc_module_provider.default {
  constructor() {
    super(...arguments);
    this.name = "sfdx-module-provider";
  }
  async getModuleEntry({specifier}) {
    if (!specifier.startsWith(SFDX_SCOPE)) {
      return void 0;
    }
    const {name} = (0, import_shared_utils.explodeSpecifier)(specifier);
    const [component, resource] = name.split("#");
    const entry = import_path.default.join(MODULE_DIR, component, resource ?? `${component}.js`);
    const parentDir = import_path.default.dirname(entry);
    if (!import_fs.default.existsSync(parentDir)) {
      import_diagnostics.logger.warn({
        label: `local-dev`,
        message: `Missing source files for module '${specifier}' at path '${entry}'`
      });
      return void 0;
    }
    return {
      id: specifier,
      specifier,
      entry,
      version: "",
      scope: process.cwd()
    };
  }
};
var sfdx_module_provider_default = SFDXProvider;
