import { LightningElement } from 'lwc';
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
import { initHMR } from 'lwr/hmr';
/**
 * The local-dev server has no way to run bootstrap services, so
 * bootstrap work is done in the connectedCallback of this root component
 *
 * This component is added to globalThis.LWR.rootComponents in *view_bootstrap.ts
 * As a component, it has no access to client bootstrap config (ie: globalThis.LWR)
 */
export default class LocalDev extends LightningElement {
    connectedCallback() {
        initHMR('/1/hmr/amd/1?debug=true', {}, false);
    }
}
//# sourceMappingURL=bootstrap.js.map